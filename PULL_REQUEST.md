# PR: Initial project setup

## Summary
- bootstrap the AI SDK proof of concept with `app.mjs` using `@ai-sdk/openai` and the `ai` helper
- document contributor practices in `AGENTS.md`
- capture package metadata (`package.json`, `pnpm-lock.yaml`) and ignore secrets and dependencies via `.gitignore`

## Testing
- `pnpm install`
- `node app.mjs`
